target/
node_modules/
dist/
app/src-tauri/gen/
*.log

# Never commit anything that could be a real secret.
*.pem
*.key
.env
.env.*

# Bundled Tor daemon, fetched at build time by CI (see release.yml)
app/src-tauri/tor/
