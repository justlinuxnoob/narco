{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Narco window. Without this the webview has no permissions, so listen() is denied and the UI never receives connection-status events from the backend — which is why the app sat on 'joining tor…' forever. Emit is deliberately absent: nothing in Rust listens for webview events, and granting it would let any script in the page forge 'ready'/'message'/'ended' events to itself.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
